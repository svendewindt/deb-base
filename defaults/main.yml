---
# defaults/main file for deb-base

# Package management
install_core_packages: ['sudo', 'locate', 'figlet', 'ssh', 'ufw', 'git']
remove_core_packages: ['firewalld', 'apparmor', 'selinux-basics', 'python-selinux', 'selinux-policy-default']

install_packages: []
remove_packages: []

# Users
add_groups: []
remove_groups: []
add_users: []
remove_users: []

ssh_keys: []

# Security

# Remove unused account, ie games
remove_unused_accounts: ['games']

